# 3.9.6 验证记录

日期：2026-09-18。功能回归与界面验收在隔离目录运行；经用户授权的正式牧场迁移结果见文末。

- 完整回归：1713 通过、6 项按环境条件跳过。随后新增“无对齐信息查看草稿已有截图”用例；相关截图和退出测试另行通过。
- 静态检查：按项目 CI 范围运行 `ruff check cowmata_tailring tests`，通过。
- Windows Computer Use：实际系统目录选择器 Ctrl+A 选择 20 个总目录，扫描得到 400 个视角、2000 个短 MP4 测试文件；界面 50 ms 心跳最大间隔 0.212 秒，99 百分位 0.071 秒。此为当前机器的目录发现实测，不构成任意硬盘条件下的性能承诺。
- Windows Computer Use：关闭空闲归类窗口后目录、表格及原始录像表单清空；实际标注列表选中视频草稿，打开已保存的原片证据图，草稿状态保持 video_draft，未确认真值。
- 自动回归覆盖重选/追加/取消/清空、空视角、恢复任务一致性、运行中关闭保留任务、历史标签只读和模型精确代码绑定、独立 XYZ 曲线。
- 授权：正式服务器管理员密码登录、角色权限获取、会话刷新、退出通过。隔离服务回归验证中文 GBK 管理员名册、UTF-8 BOM、错误表头拒绝、4 个管理员并发会话、本地伪造 CSV 不可授权；操作员授权与权限回归通过。
- 上传器：同步正式 1.4.0 安全模块与服务部署模块；不改变三份业务 CSV 字段或 Pro 台账筛选逻辑。
- 文件搬运：同盘 256 MiB 文件，共用复制函数在两次调用中分别 2.285 秒 / 2.335 秒，约 112.03 / 109.62 MiB/s；目标 SHA-256 校验一致。并未把这个数字当作原始录像的全流程转码速度。

发布包与升级的最终校验结果随交付核对记录提供。自动化测试和界面抽测不能证明任意输入、任意环境下零错误；本次没有删除正式原始数据或重新训练全部模型；正式录像目录通过同盘重命名调整，不重新转码。

## 共享录像与多人协作补充验证

- 三类 JSON 使用独立的合法 Motion、PPG、Temp 测试载荷，三份 CSV 与设备牛号、健康类别、日期对应；真实 MP4 片段用于读取和播放验证。测试标签仅用于验证数据流，不作为训练真值。
- Computer Use 实际完成原始 ZIP 打开、主界面加载共享录像、保存视频草稿、生成回传 ZIP、接收和重复接收。首次接收 1 份，第二次重复 1 份、写入 0 份。
- 派包界面：缺录像被拦截；补齐后成功生成三个完整包，重新扫描显示已派发 1 次且默认不再勾选。本机 100 ms 界面心跳最大间隔约 0.440 秒。此为隔离小样本检查，不是 TB 级压缩耗时承诺。
- 自动回归覆盖三模态缺项、无效 JSON、身份和日期冲突、缺录像、录像不覆盖、CSV 中途改动、下载中断/失败/运行、历史下载记录保留、新增资料待补派、完成标注转复核并保留标签基线。
- 安全接收覆盖 ZIP 路径越界、重复条目、未经授权来源、原始 SHA-256 不符、事件越界和当前标签范式；冲突整包待核对、异常发布回滚、中断后恢复。
- 正式牧场迁移预检查：15,608 个视频，4,189,740,597,248 字节，无目标重名；257 个元数据文件完成离线转换预演。正式迁移结果单独保存在牧场“科牧特_协作标注／目录迁移记录”。

最终扩展版本完整回归：1769 通过、6 项环境条件跳过，全部批次退出码为 0。

正式迁移完成：15,608 个视频的大小、修改时间与文件身份全部保持；199,256 个原始 JSON 的路径及文件状态不变，257 个标注/索引元数据文件已调整。共享日期、共享视角及中断恢复追加测试通过。243 份历史标注正常关联；2 份旧人工标签原本无原件绑定，另 10 份旧标签的原件此前已转入待核对，已逐一核验 SHA-256 一致，保留为待核对，不自动恢复健康类别或充当训练真值。

- 正式迁移后的 Computer Use 历史回看：实际 35 条标签、7 个录像视角加载及播放、温度切换通过；当前时间段无匹配 PPG 时明确提示缺项。7 路旧录像同时解码时触发主路优先、辅路预览，未承诺所有视角全帧播放。
- 正式历史标注只读抽样导出：行为识别数据集 3 对、产犊数据集 1 对，导出错误数均为 0，源标注 SHA-256 不变。这是数据流验证，不是模型准确率评估。
- CSV 刷新回执按记录 ID 统计新增、更新、移除，保存核验时间；测试覆盖无变更、旧表格式重建和失败提示。
