# CoWmata Pro 4.2.9

- 修复标注工作区状态提示标签未加入布局导致的大面积遮挡。状态提示现在固定在底部操作栏，高度受限，视频、波形、确认真值和补充证据按钮保持可用。
- 视频索引检查改为有界的 16 路并行读盘；当前标注窗口和后台完整索引均使用独立读取通道，避免所有任务排队或无限制提交。可通过 COWMATA_INDEX_WORKERS 在 1–16 之间调节。
- 对 F:/扬大_高邮牧场 做了只读全局核查：发现 18,877 个视频文件、索引库 3,702 条记录，其中 15,208 个文件尚未出现在数据库路径中；2026-08-23/视角01 示例文件均可在录像目录找到，但部分仍停留在 pending，待新版后台索引重新检查。审计没有修改原始录像或索引库。
- 版本号更新为 4.2.9，提供源码包、便携包和安装包。GitHub Release 仅上传安装包。

验证：索引并发单元测试 2 passed；界面遮挡几何冒烟测试通过；构建后会再次执行便携版自检和启动自检。
