# 3.9.1 本地验证记录

验证日期：2026-09-16。对象：3.9.1（shortcuts-paths-tls-391-20260916）。

## 已完成

- Windows / Python 3.13 的回归测试：**1321 通过，8 跳过**，283.80 秒。
- 按项目 CI 配置执行 Ruff：cowmata_tailring 与 tests 全部通过。额外扫描整个仓库时，归档素材与辅助脚本仍有既存风格问题；不将其计为全仓库零告警。
- 主页面和候选窗口用 Qt 按键事件测试所有 15 个标签快捷键，包括连续操作、区间结束、点事件、打开旧标签工程、跨记录保存和 F 键冲突。
- 检查主页面及候选窗口实际渲染截图：打开旧工程后当前标签显示“起立过程”，历史标签及其事件保留。
- 检查下载配置迁移、CSV 编码补全和目录规范：完整编码保持原样，仅补全简写，已配置的其他牧场目录保留。
- 验证空白或不存在的训练目录不会生成模型输出目录。
- 更新故障注入测试覆盖 TLS EOF、正文读取中断、证书错误不重试、Windows 通道切换、跳转域名限制、Range 头及断点读取。
- 本机 WinHTTP 初始化、超时配置、TLS 配置与句柄关闭成功。
- 便携包离线自检成功：使用包内 Python、Qt、VLC、FFmpeg、FFprobe；导入数值和识别依赖，构建三种布局，加载本地 OCR，验证数据集写入锁。自检时屏蔽 Python 网络及外部工具搜索路径。
- 主启动器版本启动退出码为 0，包内版本读取为 3.9.1。

## 尚未完成的验证与处理

- **真实公网更新检查和完整附件下载未通过本轮验收**：当前任务的网络沙箱拒绝连接；正常权限执行工具又因 Windows CET 错误无法启动。模拟通过不代表当前网络已恢复。需要恢复权限后补做官方发布查询、真实下载、续传及 SHA-256 验证。
- 一个直接在 F 盘根目录创建临时安装目标的测试因写入范围限制未执行；其余更新及安装事务测试随回归执行。
- 8 个跳过测试按测试环境条件跳过，本记录不将它们计为通过。
- 便携包自检使用离屏窗口，未声称在所有真实设备、全部历史视频和所有现场任务上完成了人工验收。
- F:\牛舍 下已有误下载数据共 47763 个 JSON，保持原位，未删除或合并。本轮修复新任务的保存规则。数据迁移需要先确认目标路径和同名文件冲突。
- 用户文本中的 F:\扬大\_高邮牧场 不存在；本地已有原目录为 F:\扬大_高邮牧场。本次按“原有目录”使用已有路径。

## 交付边界

仅生成源码 ZIP、便携 ZIP 及便携解压目录，不生成安装器，不上传 GitHub。不包含用户模型、现场数据、凭据及测试输出。ZIP 生成后逐项比较 SHA-256，交付目录附带校验值。
