# 3.9.0 验证记录

验证日期：2026-09-16。平台：Windows x64，分发包内 Python 3.13.15、PySide6 6.11.2、XGBoost 3.4.1。仅记录实际执行的检查，不把模拟数据结果当作牧场效果。

## 自动化回归

完整运行库和 NSIS 组件下，全套测试 **1,298 项通过，零失败、零跳过**，耗时 354.33 秒。此前跳过的原生安装目录和旧播放器界面用例已在最终一轮全部实测通过。

通过范围包括：

| 功能 | 验证内容 |
|---|---|
| 数据准备 | CSV 身份、完整与简写编码、佩戴时间、类别、三模态、冲突、路径、断点与内容校验 |
| 素材与工程 | 索引、视频探测、媒体工具、Motion/PPG、工程锁、标签与历史保存 |
| 标注复核 | 一次对齐、候选与手动确认、标签编辑、历史、协作与导出 |
| 数据集 | Raw/Label 配对、完整原始内容、所选片段、增量与身份一致性 |
| 行为 | 六类行为、按模态输入、手动模型导入、训练记录、分组评价、识别与候选共用 |
| 决策 | 因果窗口、缺失指标、结局时间、牛分组、训练折填补、模型输入版本与评价 |
| 更新 | REST 403 回退、安装版/便携版选择、续传、签名刷新、SHA、非法 ZIP、目录保护、失败回滚 |
| 界面 | 菜单次序、训练/识别页签、工作线程、取消、快捷方式、翻译覆盖 |

216 个产品 Python 文件通过 Python 3.10 语法解析；与 GitHub CI 相同的完整 Ruff 检查通过。源码声明兼容范围与完整运行环境验收范围分别记录。

## 真实服务和数据

- 已通过上传器对应连接读取三个台账文件，复核 CSV 本地目录与服务器受限协议目录的区别。
- 解析现场台账：129 个设备、776 条佩戴记录、278 个登记产犊牛号，26 条需核对信息保留为问题清单。
- 实际读取服务端 Motion、PPG、温度，使用 `546C50CA090E-23060-I` 的统一目录流转。
- 原始九轴记录 179,722 个样本；PPG 9,000 个样本；温度字段实际读取为 29.94。
- 实际索引、PPG 波形窗口、保存关闭和综合证据生成成功。
- 私人台账、密钥和原始业务样本保留在本地验证位置，没有加入发行包。

## 媒体与完整便携运行库

在程序私有 Python 下禁用 Python 网络连接，并将 PATH 限于系统目录，完成运行库导入、三种工作台布局、VLC 3.0.23、私有 FFmpeg/FFprobe 和本地 OCR 加载。

实际录像取帧为 1920 × 1080，时间戳识别为 `2026-08-11 00:01:31`。视频 SHA-256 前后相同。首次 CPU OCR 识别较慢；该检查验证正确性，不作为实时识别速度承诺。

原始九轴完整嵌入与字节一致性通过；所选 201 样本片段保留独立历史与父记录起点，不捏造标定。便携包不依赖安装到系统的 Python。

## 行为训练、识别与图文界面

六个行为分别在 Motion、PPG 上训练并手动导入，共 **12 个行为与模态组合**；每组使用 6 条测试记录，实际输出识别事件。两种模态均经“自动生成候选”的真实数值模型适配器验证。

训练数据是专门构造的功能样例，用于检查训练、保存、导入、识别、候选衔接。教程中的召回图属于这些样例，不能推断现场精确率。

教程中的下载、训练、训练图表、识别、综合证据、决策训练及工作台图像均从本版实际窗口采集。用于截图的牛号和记录为示例。

## 真实更新链路

已从 GitHub 官方发行资产下载 660,576,591 字节的既有 3.8.3 便携包，先暂停再续传，完成 SHA-256 核验。其摘要为：

`69927d6cac7a6c7f2c84eae5c041e82d16bc96e6223560f6ab394b7e5968b67d`

在独立 QA 旧目录实际运行完整替换事务：校验 8,902 个文件，导入新运行库，原地替换成功，外部标签内容保持不变，旧备份正常清理。该次事务用时 39.005 秒，验证时没有开启自动重启。3.9 安装与启动验收另行记录。

## 数据和模型保留边界

现场目录 `F:\牛舍\_现场记录` 只保存三个业务 CSV，同步状态与备份独立保存。行为和决策模型位于 `F:\科牧特\_模型`，未加入源码、安装包或便携包；通用 OCR 资源作为运行组件保留。

本次测试验证功能与文件处理，不声称六类行为或产犊风险已经完成独立牧场临床/生产效果验证。发情、疫病等待验证扩展方向不算作已交付预测模型。

## 3.9 安装、升级、自动重启与卸载

使用实际 Windows 安装候选包完成以下流程：

| 流程 | 结果 |
|---|---|
| 全新安装到独立中文目录 | 完整清单核验通过，实际打开工作台并正常关闭 |
| 旧版本运行库目录运行新版 Setup | 完成解包、6,073 个文件校验、运行库导入、替换与版本注册，约 48 秒 |
| 独立更新程序升级并自动重启 | 替换、注册与自动启动成功，检测到真实 `COWMATA Pro™ · 行为真值标注` 窗口，正常关闭 |
| 新装后、离线升级后、自动更新后卸载 | 三次均只移除程序文件，保留放入安装目录的测试标签文件 |
| 本机既有安装登记 | 原 3.8.3 登记位置保持不变 |

旧运行库验收目录来自实际 3.8.3 便携发行文件，以隔离的 3.8.0 安装身份测试升级，避免更改用户现有 3.8.3 登记。自动重启另使用带真实启动器的受管理旧版本夹具。所有验收目录与现场工程隔离。

安装候选包 SHA-256：`d05fdf29a1ede81351315d880522a341b5ab5593902e177920447a663b3cf7d8`。最终发行文件在文档补齐后重新构建，其摘要以 Release 资产和交付文件校验为准。
