# 3.9.9 验证记录

## 自动化验证

- 转码、边界封装、音频时钟修复和并行调度定向测试：20 passed。
- 完整回归中新增的边界封装和 H.265 模式测试均通过；一次完整回归中的计划读取耗时测试受并行桌面进程争用影响，单独复测通过。
- 发行前必须再次运行完整 `run_tests.py -q`，并保存最终输出；安装包只从该次通过的干净构建目录生成。

## 实际媒体测量

- 代表性 30 秒 HEVC 片段：原码流快速封装约 11.97 MB；H.265 QSV 节省空间模式（质量 35）约 10.19 MB，约小 14.9%。该比例只代表样本，不作为所有录像的固定比例。
- 原码流封装不解码、不编码，耗时主要由读取、写入和校验决定；边界片段也不再为删除几秒而编码整段录像。

## 判定

3.9.9 的默认路径是原码流封装，容量模式是显式选择项。20 路任务会同时调度，物理原盘读取仍限为单读，以保证长时间运行稳定。
